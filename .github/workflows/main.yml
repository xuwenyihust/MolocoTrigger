name: Run Main.py

on:
  push:
    branches:
      - main

jobs:
  run_main:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run main.py
      run: python /Users/wenyixu/Coding/MolocoTrigger/main.py
